package com.irisvpn.android.androidSpecific

import com.irisvpn.android.utils.vpn.AdService
import com.irisvpn.android.utils.vpn.AddState

class GoogleAdService: AdService {

    override fun showAdd(callBack: (AddState) -> Unit) {

    }
}